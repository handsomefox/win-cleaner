[advisories]
# wayland-scanner uses quick-xml only at build time to parse trusted XML bundled
# with the Wayland protocol crates. Remove these exceptions when wayland-scanner
# accepts quick-xml >= 0.41.
ignore = [
    "RUSTSEC-2026-0194",
    "RUSTSEC-2026-0195",
]
